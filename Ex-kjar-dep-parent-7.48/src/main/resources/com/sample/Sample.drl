package com.sample
 
import com.sample.Person;
 
rule "Hello"
    when
        $p : Person( age > 10 )
    then
        System.out.println( "AA1 Hello, " + $p.getName());
end


rule "Hello2"
    when
        $p : Person( age > 10 )
    then
        System.out.println( "AA2 Hello, " + $p.getName());
end